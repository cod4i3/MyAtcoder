#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;
#define MOD 1000000007

int main() {
  int H, W;
  cin >> H >> W;
  vector<vector<int> > red(H);
  vector<vector<int> > blue(H);

  for (int i = 0; i < H; i++) {
    int a;
    cin >> a;
    red[i].push_back(a);
  }

  for (int i = 0; i < H; i++) {
    int b;
    cin >> b;
    blue[i].push_back(b);
  }

  vector<int> route;
  for (int i = 0; i < N; i++) {
  }
}