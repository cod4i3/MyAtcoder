#include <iostream>
#include <vector>
using namespace std;

int main() {
  int N;
  cin >> N;
  vector<vector<int>> P(N, vector<int>(N, -1));
  bool seen[N][N];

  for (int i = 0; i < N; i++) {
    int p;
    cin >> p, p--;
    seen
  }
}