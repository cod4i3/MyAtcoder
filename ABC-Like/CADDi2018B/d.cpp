#include <iostream>
#include <vector>
#include <algorithm>
using ll = long long;
using namespace std;


int main(){
    int N;
    cin >> N;
    vector<ll> a(N);
    for(int i=0; i<N; i++) cin >> a[i];
    
}