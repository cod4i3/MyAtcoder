#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

int main() {
  int R, C, K;
  cin >> R >> C >> K;
  vector<string> s(R);
  for (int i = 0; i < R; i++) cin >> s[i];
  
}